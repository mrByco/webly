import { TestBed } from '@angular/core/testing';
import { App } from './app';

describe('App', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [App],
    })
      .compileComponents();
  });

  // The scaffold's second test asserted on a "Hello, client" heading from `ng new`. The root
  // component has rendered a router outlet since the first real screen landed, so it had been
  // failing ever since and was testing nothing.
  it('should create the app', () => {
    const fixture = TestBed.createComponent(App);
    const app = fixture.componentInstance;
    expect(app).toBeTruthy();
  });
});
