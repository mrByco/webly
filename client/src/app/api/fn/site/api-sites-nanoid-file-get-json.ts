/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SiteFileResponse } from '../../models/site-file-response';

export interface ApiSitesNanoidFileGet$Json$Params {
  nanoid: string;
  path?: string;
  version?: string;
}

export function apiSitesNanoidFileGet$Json(http: HttpClient, rootUrl: string, params: ApiSitesNanoidFileGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<SiteFileResponse>> {
  const rb = new RequestBuilder(rootUrl, apiSitesNanoidFileGet$Json.PATH, 'get');
  if (params) {
    rb.path('nanoid', params.nanoid, {});
    rb.query('path', params.path, {});
    rb.query('version', params.version, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SiteFileResponse>;
    })
  );
}

apiSitesNanoidFileGet$Json.PATH = '/api/sites/{nanoid}/file';
